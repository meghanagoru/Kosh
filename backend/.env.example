# Uvicorn (ASGI)
UVICORN_HOST=0.0.0.0
UVICORN_PORT=5000
UVICORN_WORKERS=2

# App
SECRET_KEY=change-me-to-a-long-random-string-in-production

# MongoDB (use service hostname `mongo` when running in Docker Compose)
MONGODB_URI=mongodb://mongo:27017/kosh
